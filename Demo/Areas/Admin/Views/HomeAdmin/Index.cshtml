

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Line Chart Example</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        canvas {
            width: 50%;
            height: 50%;
        }
    </style>
</head>
<body>
    <canvas id="myChart"></canvas>

    <script>
        // Lấy tham chiếu canvas
        var ctx = document.getElementById('myChart').getContext('2d');

        // Khởi tạo biểu đồ đường
        var myChart = new Chart(ctx, {
            type: 'line', // Loại biểu đồ: đường
            data: {
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5', 'Tháng 6'], // Nhãn cho trục x
                datasets: [{
                    label: 'Doanh thu', // Nhãn cho dữ liệu
                    data: [5000, 2000, 4000, 8000, 9000, 1000], // Dữ liệu cho các điểm trên đường
                    fill: false, // Bật/tắt tô màu vùng dưới đường
                    borderColor: 'red', // Màu đường viền
                    borderWidth: 2, // Độ rộng đường viền
                    pointRadius: 5, // Bán kính điểm dữ liệu
                    pointBackgroundColor: 'red', // Màu nền điểm dữ liệu
                    pointBorderColor: 'white', // Màu viền điểm dữ liệu
                    pointHoverRadius: 10, // Bán kính điểm dữ liệu khi di chuột qua
                    pointHoverBackgroundColor: 'yellow', // Màu nền điểm dữ liệu khi di chuột qua
                    pointHoverBorderColor: 'black' // Màu viền điểm dữ liệu khi di chuột qua
                }]
            },
            options: {
                title: {
                    display: true,
                    text: 'Doanh thu theo tháng (VND)'
                },
                scales: {
                    y: {
                        beginAtZero: true // Bắt đầu trục y từ 0
                    }
                },
                layout: {
                    padding: {
                        left: 10,
                        right: 10,
                        top: 10,
                        bottom: 10
                    }
                }
            }
        });
    </script>
</body>
</html>

@* 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pie Chart Example</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        canvas {
            width: 20%;
            height: 30%;
        }
    </style>
</head>
<body>
    <canvas id="myChart"></canvas>

    <script>
        // Lấy tham chiếu canvas
        var ctx = document.getElementById('myChart').getContext('2d');

        // Khởi tạo biểu đồ tròn
        var myChart = new Chart(ctx, {
            type: 'pie', // Loại biểu đồ: tròn
            data: {
                labels: ['Hamburger', 'Spagetti', 'Gà rán', 'Tráng miệng', 'Thức uống'], // Nhãn cho các phần
                datasets: [{
                    data: [20, 30, 40, 25, 15], // Giá trị cho các phần
                    backgroundColor: ['red', 'blue', 'yellow', 'green', 'purple'], // Màu nền cho các phần
                    borderColor: ['black', 'black', 'black', 'black', 'black'], // Màu viền cho các phần
                    borderWidth: 1 // Độ rộng viền cho các phần
                }]
            },
            options: {
                title: {
                    display: true,
                    text: 'Tỷ lệ đặt loại món ăn'
                }
            }
        });
    </script>
</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Area Chart Example</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        canvas {
            width: 800px;
            height: 400px;
        }
    </style>
</head>
<body>
    <canvas id="myChart"></canvas>

    <script>
        // Lấy tham chiếu canvas
        var ctx = document.getElementById('myChart').getContext('2d');

        // Khởi tạo biểu đồ miền
        var myChart = new Chart(ctx, {
            type: 'line', // Loại biểu đồ: đường (đối với biểu đồ miền cần sử dụng loại line)
            data: {
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5'], // Nhãn cho trục x
                datasets: [{
                    label: 'Doanh thu', // Nhãn cho dữ liệu
                    data: [7000, 4000, 7000, 6000, 2000], // Giá trị cho các điểm trên đường
                    fill: true, // Bật tô màu vùng dưới đường (để tạo hiệu ứng miền)
                    backgroundColor: 'rgba(255, 99, 132, 0.2)', // Màu nền cho vùng miền
                    borderColor: 'red', // Màu đường viền
                    borderWidth: 2, // Độ rộng đường viền
                    pointRadius: 5, // Bán kính điểm dữ liệu
                    pointBackgroundColor: 'red', // Màu nền điểm dữ liệu
                    pointBorderColor: 'white', // Màu viền điểm dữ liệu
                    pointHoverRadius: 10, // Bán kính điểm dữ liệu khi di chuột qua
                    pointHoverBackgroundColor: 'yellow', // Màu nền điểm dữ liệu khi di chuột qua
                    pointHoverBorderColor: 'black' // Màu viền điểm dữ liệu khi di chuột qua
                }]
            },
            options: {
                title: {
                    display: true,
                    text: 'Doanh thu theo tháng'
                },
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true // Bắt đầu trục y từ 0
                        }
                    }]
                }
            }
        });
    </script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Column Chart Example</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <canvas id="myChart" style="width: 650px; height: 400px;"></canvas>

    <script>
        // Lấy tham chiếu canvas
        var ctx = document.getElementById('myChart').getContext('2d');

        // Khởi tạo biểu đồ cột
        var myChart = new Chart(ctx, {
            type: 'bar', // Loại biểu đồ: cột
            data: {
                // labels: ['Thứ 2', 'Thứ 3', 'Thứ tư', 'Thứ 5', 'Thứ sáu', 'Thứ 7', 'CN'], // Nhãn của các cột
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5'], // Nhãn của các cột
                datasets: [{
                    label: 'Doanh thu(Đơn)', // Nhãn của dữ liệu
                    data: [40, 50, 60, 70, 80], // Dữ liệu của các cột
                    backgroundColor: [
                        'teal',
                        'teal',
                        'teal',
                        'teal',
                        'teal',


                    ],
                    borderColor: [
                        'black',
                        'black',
                        'black',
                        'black',
                        'black',

                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true // Bắt đầu trục y từ 0
                    },
                    xAxes: [{ // Thay đổi cho trục x
                        ticks: {
                            autoSkip: false // Hiển thị tất cả các nhãn
                        },
                        rotation: 45 // Xoay nhãn 45 độ
                    }]
                },
                layout: {
                    padding: {
                        left: 10,
                        right: 10,
                        top: 10,
                        bottom: 10
                    }
                },
                indexAxis: 'x',
                elements: {
                    bar: {
                        borderWidth: 2,
                        borderSkipped: 'start',
                        borderRadius: 1,
                        borderAlign: 'center',
                        barWidth: 0.6
                    }
                }
            }
        });
    </script>
</body>
</html> *@


@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Column Chart Example</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <canvas id="myChart" style="width: 650px; height: 400px;"></canvas>

    <script>
        // Lấy tham chiếu canvas
        var ctx = document.getElementById('myChart').getContext('2d');

        // Khởi tạo biểu đồ cột
        var myChart = new Chart(ctx, {
            type: 'bar', // Loại biểu đồ: cột
            data: {
                // labels: ['Thứ 2', 'Thứ 3', 'Thứ tư', 'Thứ 5', 'Thứ sáu', 'Thứ 7', 'CN'], // Nhãn của các cột
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5'], // Nhãn của các cột
                datasets: [{
                    label: 'Doanh thu(Đơn)', // Nhãn của dữ liệu
                    data: [ 40, 50, 60, 70, 80], // Dữ liệu của các cột
                    backgroundColor: [
                        'teal',
                        'teal',
                        'teal',
                        'teal',
                        'teal',


                    ],
                    borderColor: [
                        'black',
                        'black',
                        'black',
                        'black',
                        'black',

                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true // Bắt đầu trục y từ 0
                    },
                    xAxes: [{ // Thay đổi cho trục x
                        ticks: {
                            autoSkip: false // Hiển thị tất cả các nhãn
                        },
                        rotation: 45 // Xoay nhãn 45 độ
                    }]
                },
                layout: {
                    padding: {
                        left: 10,
                        right: 10,
                        top: 10,
                        bottom: 10
                    }
                },
                indexAxis: 'x',
                elements: {
                    bar: {
                        borderWidth: 2,
                        borderSkipped: 'start',
                        borderRadius: 1,
                        borderAlign: 'center',
                        barWidth: 0.6
                    }
                }
            }
        });
    </script>
</body>
</html>

 *@